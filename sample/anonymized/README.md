# Anonymized sample data（ローカル専用）

このディレクトリの **CSV / MBOX 等は Git 管理しません**。  
実顧客メールも、登録済みワークスペースデータもリポジトリに置かないでください。

ローカルでの動作確認用に、架空データだけをここに置いてください。

例:

```text
sample/anonymized/maildealer_sample.csv
```

単体テストはリポジトリ内のインライン fixture を使うため、この CSV がなくても実行できます。
